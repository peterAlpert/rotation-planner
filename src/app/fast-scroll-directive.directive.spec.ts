import { FastScrollDirectiveDirective } from './fast-scroll-directive.directive';

describe('FastScrollDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FastScrollDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
